{
  "name": "turnpoint-viewer",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Minimal MapLibre viewer for Turnpoint plans (decision 0003, 0005). Not for operational use.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "maplibre-gl": "^6.10.0"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}

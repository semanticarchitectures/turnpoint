# scenarios/

Scenario definitions for agent evaluation: area, data sets, constraints, objectives
and scoring rules. Format to be specified in `docs/specs/scenario-format.md`.
All scenario content is notional and uses public data only.
